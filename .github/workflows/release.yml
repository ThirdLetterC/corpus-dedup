name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
          sudo apt-get update
          sudo apt-get install -y gcc-14 g++-14 cmake nasm build-essential

      - name: Build (make release)
        run: |
          CC=gcc-14 CXX=g++-14 make release
          find . -name "corpus_dedup"

      - name: Package binary
        run: |
          binary_path="./build/release/corpus_dedup"
          if [ ! -f "${binary_path}" ]; then
            echo "Binary not found at ${binary_path}"
            exit 1
          fi
          strip "${binary_path}" || true
          tar -czf "corpus_dedup-linux-x86_64.tar.gz" -C "./build/release" corpus_dedup

      - name: Create release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            corpus_dedup-linux-x86_64.tar.gz
